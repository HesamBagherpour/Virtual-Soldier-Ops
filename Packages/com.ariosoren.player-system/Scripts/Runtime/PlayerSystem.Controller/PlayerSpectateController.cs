using UnityEngine;

namespace ArioSoren.PlayerSystem.Controller.PlayerSystem.Controller
{
    public class PlayerSpectateController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
